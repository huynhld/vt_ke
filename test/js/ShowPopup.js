'use strict';

console.log(`ShowPopup.js start`);

function ShowPopup() {}

ShowPopup.prototype.do = function(first_argument) {
    // body...
    console.log(`ShowPopup: ` + first_argument);
};
console.log(`ShowPopup.js end`);
